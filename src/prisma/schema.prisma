datasource db {
    provider = "mongodb"
    url = "mongodb+srv://test:xYa5TgxDqMHD5ixF@cluster0.55malts.mongodb.net/"
}


generator client{
    provider = "prisma-client-js"
    previewFeatures = ["mongoDb"]
}

model User {
    id String @id @default(dbgenerated()) @Map("_id") @db.ObjectId
    email String unique
    name String
    password String
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
}